function myFunction() {
  
	document.getElementById("p2").style.backgroundColor = "blue";
  }

/*function confirmAction() {
  if (confirm("Are you sure you want to perform this action?")) {
    // User clicked OK, perform the action here
    alert("Action performed successfully!");
  } else {
    // User clicked Cancel, do nothing
  }
}







/* // Get the div element with id "myDiv"
const myDiv = document.getElementById("myDiv");

// Define the function that changes the background color of the div
function changeBackground() {
  myDiv.style.backgroundColor = "#8ACEE8";
}

// Add a click event listener to the div that calls the changeBackground function
myDiv.addEventListener("click", changeBackground);
 */